# Tekoälyvalmentaja Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Uusi keskusteleva tekoälyvalmentaja-sivu, joka vastaa käyttäjän kysymyksiin lukemalla oikeaa sovellusdataa (Sali, Aerobinen, Keho, Uni, Ruoka) ja antaa perusteltuja ehdotuksia — ei koskaan muuta dataa itse.

**Architecture:** Selain kutsuu uutta Supabase Edge Functionia (`coach-chat`), joka hakee tuoreen datayhteenvedon palvelinpuolella, kutsuu Claude APIa ja palauttaa vastauksen. Keskusteluhistoria tallennetaan uuteen `coach_messages`-tauluun; selain vastaa sekä käyttäjä- että vastausviestien tallennuksesta. Kaksi lisäsuojausta joita muualla sovelluksessa ei ole: salasanaportti (`x-coach-secret`-header) ja päivittäinen viestilaskuri, koska tämä on ensimmäinen ominaisuus jossa väärinkäyttö maksaisi oikeaa rahaa.

**Tech Stack:** Supabase Edge Function (Deno, TypeScript), Claude API (Anthropic), olemassa oleva Supabase-taulukanta, vanilla JS + Supabase JS -asiakas (`index.html`).

---

### Task 1: Tietokantamigraatio — `coach_messages`

**Files:**
- Create: `supabase/migrations/20260714_coach_messages.sql`

- [ ] **Step 1: Kirjoita migraatio**

```sql
create table coach_messages (
  id              bigint generated by default as identity primary key,
  conversation_id uuid not null,
  role            text not null check (role in ('user','assistant')),
  content         text not null,
  created_at      timestamptz not null default now()
);

alter table coach_messages enable row level security;

create policy coach_messages_select on coach_messages
  for select to anon, authenticated using (true);
create policy coach_messages_insert on coach_messages
  for insert to anon, authenticated with check (true);
```

- [ ] **Step 2: Sovella migraatio**

Aja `supabase db query --linked -f supabase/migrations/20260714_coach_messages.sql` (jos CLI on linkitetty projektiin), tai pyydä käyttäjää ajamaan SQL Supabase SQL-editorissa. Vahvista onnistuminen:

```bash
supabase db query --linked --data-only -c "select count(*) from coach_messages;"
```

Odotettu tulos: `0` (taulu on tyhjä mutta olemassa, ei virhettä).

- [ ] **Step 3: Committaa**

```bash
git add supabase/migrations/20260714_coach_messages.sql
git commit -m "feat: coach_messages-taulu tekoälyvalmentajaa varten"
```

---

### Task 2: Edge Function — turvallisuus + keskustelurunko (ei vielä datakontekstia)

**Files:**
- Create: `supabase/functions/coach-chat/index.ts`

Tämä versio vastaa kysymyksiin PELKÄN keskusteluhistorian perusteella (ei vielä lue käyttäjän oikeaa dataa) — täysin toimiva, deployattava ja testattava tila, ennen kuin Task 3 lisää datakontekstin.

- [ ] **Step 1: Kirjoita Edge Function**

```typescript
import { createClient } from 'npm:@supabase/supabase-js@2';

const SB_URL = Deno.env.get('SUPABASE_URL')!;
const SB_SERVICE_KEY = Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!;
const COACH_SECRET = Deno.env.get('COACH_SECRET')!;
const ANTHROPIC_API_KEY = Deno.env.get('ANTHROPIC_API_KEY')!;
const DAILY_MESSAGE_LIMIT = 100;

const CORS_HEADERS = {
  'Access-Control-Allow-Origin': '*',
  'Access-Control-Allow-Headers': 'authorization, apikey, content-type, x-coach-secret',
};

const COACH_SYSTEM_PROMPT = `Olet Valkku-sovelluksen henkilökohtainen valmentaja. Käyttäjä harjoittelee salilla ja kestävyysurheilua, seuraa unta, painoa ja ruokailua.

Säännöt:
- Anna ehdotuksia ja havaintoja, älä koskaan väitä tehneesi muutoksia dataan tai sovellukseen — et voi kirjoittaa mitään, vain lukea ja keskustella.
- Perusta vastauksesi annettuun dataan. Jos dataa ei ole tarpeeksi jonkin kysymyksen vastaamiseen, sano niin suoraan äläkä arvaa.
- Viittaa konkreettisiin lukuihin kun mahdollista.
- Ole ytimekäs — muutama virke riittää useimpiin vastauksiin, ellei käyttäjä pyydä pidempää analyysiä.
- Vastaa suomeksi.`;

async function callClaude(
  systemPrompt: string,
  messages: { role: string; content: string }[],
): Promise<string> {
  const res = await fetch('https://api.anthropic.com/v1/messages', {
    method: 'POST',
    headers: {
      'x-api-key': ANTHROPIC_API_KEY,
      'anthropic-version': '2023-06-01',
      'content-type': 'application/json',
    },
    body: JSON.stringify({
      model: 'claude-sonnet-5',
      max_tokens: 1024,
      system: systemPrompt,
      messages,
    }),
  });
  if (!res.ok) {
    const errText = await res.text();
    throw new Error(`Claude API error ${res.status}: ${errText}`);
  }
  const data = await res.json();
  const textBlock = (data.content || []).find((b: any) => b.type === 'text');
  return textBlock?.text || '(ei vastausta)';
}

Deno.serve(async (req) => {
  if (req.method === 'OPTIONS') {
    return new Response('ok', { headers: CORS_HEADERS });
  }
  if (req.method !== 'POST') {
    return new Response('Method Not Allowed', { status: 405, headers: CORS_HEADERS });
  }
  if (req.headers.get('x-coach-secret') !== COACH_SECRET) {
    return new Response('Unauthorized', { status: 401, headers: CORS_HEADERS });
  }

  let body: { conversation_id?: string };
  try {
    body = await req.json();
  } catch {
    return new Response('Bad Request', { status: 400, headers: CORS_HEADERS });
  }
  if (!body.conversation_id) {
    return new Response('Bad Request: conversation_id required', { status: 400, headers: CORS_HEADERS });
  }

  const sb = createClient(SB_URL, SB_SERVICE_KEY);

  const todayStart = new Date();
  todayStart.setUTCHours(0, 0, 0, 0);
  const { count: todayCount, error: countError } = await sb
    .from('coach_messages')
    .select('id', { count: 'exact', head: true })
    .eq('role', 'user')
    .gte('created_at', todayStart.toISOString());
  if (countError) console.error('daily count query failed:', countError.message);
  if ((todayCount || 0) > DAILY_MESSAGE_LIMIT) {
    return new Response('Daily message limit reached', { status: 429, headers: CORS_HEADERS });
  }

  const { data: history, error: historyError } = await sb
    .from('coach_messages')
    .select('role,content')
    .eq('conversation_id', body.conversation_id)
    .order('created_at', { ascending: true });
  if (historyError) console.error('history query failed:', historyError.message);
  if (!history || !history.length) {
    return new Response('Bad Request: no messages found for conversation', { status: 400, headers: CORS_HEADERS });
  }

  const messages = history.map((m) => ({ role: m.role, content: m.content }));

  let reply: string;
  try {
    reply = await callClaude(COACH_SYSTEM_PROMPT, messages);
  } catch (err) {
    console.error('Claude call failed:', err instanceof Error ? err.message : String(err));
    return new Response('AI request failed', { status: 502, headers: CORS_HEADERS });
  }

  return new Response(JSON.stringify({ reply }), {
    headers: { ...CORS_HEADERS, 'content-type': 'application/json' },
  });
});
```

**Huomio mallin nimestä:** `'claude-sonnet-5'` on tämänhetkinen Sonnet-tason mallitunniste. Jos Task-vaiheen testikutsu (Step 4) palauttaa virheen joka viittaa tuntemattomaan malliin, tarkista voimassa oleva mallitunniste osoitteesta `https://platform.claude.com/docs/en/about-claude/models` ja päivitä `model`-kenttä vastaavasti ennen jatkamista.

- [ ] **Step 2: Generoi ja aseta `COACH_SECRET`**

```bash
COACH_SECRET_VALUE=$(openssl rand -hex 32)
echo "Tallenna tämä myöhempää käyttöä varten: $COACH_SECRET_VALUE"
supabase secrets set COACH_SECRET="$COACH_SECRET_VALUE" --project-ref dodrzzgbdlucjbkmxbjn
```

- [ ] **Step 3: Pyydä käyttäjää asettamaan `ANTHROPIC_API_KEY`**

Tätä ei aseta puolestasi — API-avain on käyttäjän oma tunniste eikä sitä pidä liittää keskusteluun. Pyydä käyttäjää ajamaan itse (esim. `!`-etuliitteellä tässä istunnossa, tai omassa terminaalissaan):

```bash
supabase secrets set ANTHROPIC_API_KEY="<oma-avain-tähän>" --project-ref dodrzzgbdlucjbkmxbjn
```

Odota vahvistus että komento on ajettu ennen kuin jatkat.

- [ ] **Step 4: Deployaa ja testaa**

```bash
supabase functions deploy coach-chat --project-ref dodrzzgbdlucjbkmxbjn
```

(Huomaa: EI `--no-verify-jwt`-lippua — tämä functio vaatii tavallisen Supabase-anon-key-todennuksen, toisin kuin `check-and-notify`.)

Testaa käsin lisäämällä ensin testiviesti tauluun suoraan, sitten kutsumalla functiota:

```bash
SB_URL="https://dodrzzgbdlucjbkmxbjn.supabase.co"
SB_KEY="<anon key index.html:sta>"
COACH_SECRET_VALUE="<Step 2:ssa generoitu arvo>"
TEST_CONV_ID=$(python3 -c "import uuid; print(uuid.uuid4())")

curl -s -X POST "$SB_URL/rest/v1/coach_messages" \
  -H "apikey: $SB_KEY" -H "Authorization: Bearer $SB_KEY" -H "Content-Type: application/json" \
  -d "{\"conversation_id\":\"$TEST_CONV_ID\",\"role\":\"user\",\"content\":\"Terve, kuka olet?\"}"

curl -s -X POST "$SB_URL/functions/v1/coach-chat" \
  -H "apikey: $SB_KEY" -H "Authorization: Bearer $SB_KEY" \
  -H "x-coach-secret: $COACH_SECRET_VALUE" -H "Content-Type: application/json" \
  -d "{\"conversation_id\":\"$TEST_CONV_ID\"}"
```

Odotettu tulos: JSON-vastaus `{"reply": "..."}` jossa AI esittäytyy valmentajana. Testaa myös että väärä `x-coach-secret` palauttaa `401` ja puuttuva `conversation_id` palauttaa `400`.

**Siivoa testidata:**

```bash
curl -s -X DELETE "$SB_URL/rest/v1/coach_messages?conversation_id=eq.$TEST_CONV_ID" \
  -H "apikey: $SB_KEY" -H "Authorization: Bearer $SB_KEY"
```

- [ ] **Step 5: Committaa**

```bash
git add supabase/functions/coach-chat/index.ts
git commit -m "feat: coach-chat Edge Function — turvallisuus ja keskustelurunko"
```

---

### Task 3: Edge Function — datakonteksti

**Files:**
- Create: `supabase/functions/coach-chat/context.ts`
- Modify: `supabase/functions/coach-chat/index.ts`

- [ ] **Step 1: Kirjoita datakonteksti-moduuli**

```typescript
import { createClient } from 'npm:@supabase/supabase-js@2';

type SB = ReturnType<typeof createClient>;

function isoDaysAgo(days: number): string {
  const d = new Date();
  d.setUTCDate(d.getUTCDate() - days);
  return d.toISOString().slice(0, 10);
}

function mondayOfWeeksAgo(weeksAgo: number): Date {
  const now = new Date();
  const dow = now.getUTCDay();
  const diff = dow === 0 ? -6 : 1 - dow;
  const monday = new Date(now);
  monday.setUTCDate(now.getUTCDate() + diff - weeksAgo * 7);
  monday.setUTCHours(0, 0, 0, 0);
  return monday;
}

function isoDate(d: Date): string {
  return d.toISOString().slice(0, 10);
}

function calcAge(birthDateIso: string): number {
  const today = new Date();
  const birth = new Date(birthDateIso);
  let age = today.getFullYear() - birth.getFullYear();
  const monthDiff = today.getMonth() - birth.getMonth();
  if (monthDiff < 0 || (monthDiff === 0 && today.getDate() < birth.getDate())) age--;
  return age;
}

function calcBmr(profile: any, weightRow: any): number | null {
  if (!profile || !profile.sex || !profile.height_cm || !profile.birth_date || !weightRow) return null;
  const weight = weightRow.weight_kg;
  const height = profile.height_cm;
  if (weightRow.fat_pct != null) {
    const leanMass = weight * (1 - weightRow.fat_pct / 100);
    return Math.round(370 + 21.6 * leanMass);
  }
  const age = calcAge(profile.birth_date);
  const base = 10 * weight + 6.25 * height - 5 * age;
  return Math.round(profile.sex === 'male' ? base + 5 : base - 161);
}

export async function buildDataContext(sb: SB): Promise<string> {
  const todayIso = isoDate(new Date());
  const twelveWeeksAgoIso = isoDate(mondayOfWeeksAgo(11));
  const threeWeeksAgoIso = isoDaysAgo(21);
  const ninetyDaysAgoIso = isoDaysAgo(89);

  const [
    { data: profile },
    { data: weightRows },
    { data: gymSetsAll },
    { data: activitiesAll },
    { data: sleepAll },
    { data: foodAll },
    { data: recentSets },
    { data: activeDaysAct },
    { data: activeDaysGym },
    { data: todaySessions },
    { data: appSettings },
  ] = await Promise.all([
    sb.from('user_profile').select('*').eq('id', 1).maybeSingle(),
    sb.from('body_metrics').select('weight_kg,fat_pct,measured_at').gte('measured_at', twelveWeeksAgoIso).order('measured_at', { ascending: true }),
    sb.from('workout_sets').select('workout_date').gte('workout_date', twelveWeeksAgoIso).lte('workout_date', todayIso),
    sb.from('activity_data').select('activity_type,activity_date,duration_min,distance_km,calories').gte('activity_date', twelveWeeksAgoIso).lte('activity_date', todayIso),
    sb.from('sleep_data').select('sleep_date,duration_min,deep_sleep_min,rem_sleep_min').gte('sleep_date', twelveWeeksAgoIso).lte('sleep_date', todayIso),
    sb.from('food_log_entries').select('logged_at,kcal').gte('logged_at', twelveWeeksAgoIso).lte('logged_at', todayIso),
    sb.from('workout_sets').select('workout_date,exercise_name,weight_kg,reps').gte('workout_date', threeWeeksAgoIso).lte('workout_date', todayIso).order('workout_date', { ascending: true }),
    sb.from('activity_data').select('activity_date').gte('activity_date', ninetyDaysAgoIso).lte('activity_date', todayIso),
    sb.from('workout_sets').select('workout_date').gte('workout_date', ninetyDaysAgoIso).lte('workout_date', todayIso),
    sb.from('workout_sessions').select('calories').eq('workout_date', todayIso),
    sb.from('app_settings').select('calorie_correction').eq('id', 1).maybeSingle(),
  ]);

  const lines: string[] = [];

  if (profile) {
    lines.push(`Profiili: sukupuoli ${profile.sex ?? '—'}, pituus ${profile.height_cm ?? '—'} cm, syntymäaika ${profile.birth_date ?? '—'}.`);
  } else {
    lines.push('Profiilia ei ole vielä asetettu.');
  }

  lines.push('\nViikkoyhteenvedot (viim. 12 viikkoa, uusin viimeisenä):');
  for (let w = 11; w >= 0; w--) {
    const monday = mondayOfWeeksAgo(w);
    const sunday = new Date(monday);
    sunday.setUTCDate(monday.getUTCDate() + 6);
    const from = isoDate(monday);
    const to = isoDate(sunday);

    const gymDays = new Set(
      (gymSetsAll || []).filter((r: any) => r.workout_date >= from && r.workout_date <= to).map((r: any) => r.workout_date),
    ).size;
    const weekActivities = (activitiesAll || []).filter((r: any) => r.activity_date >= from && r.activity_date <= to);
    const totalKm = weekActivities.reduce((s: number, r: any) => s + (r.distance_km || 0), 0);
    const weekSleep = (sleepAll || []).filter((r: any) => r.sleep_date >= from && r.sleep_date <= to && r.duration_min != null);
    const avgSleepH = weekSleep.length
      ? weekSleep.reduce((s: number, r: any) => s + r.duration_min, 0) / weekSleep.length / 60
      : null;
    const weekWeights = (weightRows || []).filter((r: any) => r.measured_at >= from && r.measured_at <= to);
    const weekWeight = weekWeights.length ? weekWeights[weekWeights.length - 1].weight_kg : null;

    lines.push(
      `${from}–${to}: salikäyntejä ${gymDays}, aktiviteetteja ${weekActivities.length} (${totalKm.toFixed(1)} km), ` +
      `uni keskim. ${avgSleepH != null ? avgSleepH.toFixed(1) + 'h' : '—'}, paino ${weekWeight != null ? weekWeight + ' kg' : '—'}.`,
    );
  }

  lines.push('\nTreenisessiot (viim. 3 viikkoa, liikkeittäin):');
  const setsByDateEx = new Map<string, { weight: number | null; reps: number | null }[]>();
  for (const s of (recentSets || []) as any[]) {
    const key = `${s.workout_date}|${s.exercise_name}`;
    if (!setsByDateEx.has(key)) setsByDateEx.set(key, []);
    setsByDateEx.get(key)!.push({ weight: s.weight_kg, reps: s.reps });
  }
  if (setsByDateEx.size) {
    for (const [key, sets] of setsByDateEx) {
      const [date, exName] = key.split('|');
      const setsStr = sets.map((s) => `${s.weight ?? '?'}×${s.reps ?? '?'}`).join(', ');
      lines.push(`${date} ${exName}: ${setsStr}`);
    }
  } else {
    lines.push('Ei kirjattuja sarjoja viimeisen 3 viikon aikana.');
  }

  lines.push('\nRuokailu (viim. 3 viikkoa, päiväkohtaiset kcal-summat):');
  const foodByDate = new Map<string, number>();
  for (const f of (foodAll || []) as any[]) {
    if (f.logged_at >= threeWeeksAgoIso && f.kcal != null) {
      foodByDate.set(f.logged_at, (foodByDate.get(f.logged_at) || 0) + f.kcal);
    }
  }
  if (foodByDate.size) {
    for (const [date, kcal] of [...foodByDate.entries()].sort()) {
      lines.push(`${date}: ${Math.round(kcal)} kcal`);
    }
  } else {
    lines.push('Ei ruokakirjauksia viimeisen 3 viikon aikana.');
  }

  const activeDays = new Set<string>();
  (activeDaysAct || []).forEach((r: any) => activeDays.add(r.activity_date));
  (activeDaysGym || []).forEach((r: any) => activeDays.add(r.workout_date));
  let streak = 0;
  for (let i = 0; i < 90; i++) {
    const d = new Date();
    d.setUTCDate(d.getUTCDate() - i);
    const dateStr = isoDate(d);
    if (activeDays.has(dateStr)) {
      streak++;
    } else {
      if (i === 0) continue;
      break;
    }
  }

  const correction = (appSettings && (appSettings as any).calorie_correction) ?? 1;
  const latestWeight = weightRows && weightRows.length ? weightRows[weightRows.length - 1] : null;
  const bmr = calcBmr(profile, latestWeight);
  const todayExerciseKcal =
    (activitiesAll || [])
      .filter((r: any) => r.activity_date === todayIso)
      .reduce((s: number, r: any) => s + (r.calories != null ? r.calories * correction : 0), 0) +
    (todaySessions || []).reduce((s: number, r: any) => s + (r.calories || 0), 0);
  const todayFoodKcal = (foodAll || [])
    .filter((r: any) => r.logged_at === todayIso)
    .reduce((s: number, r: any) => s + (r.kcal || 0), 0);

  lines.push('\nTämän hetken tilannekuva:');
  lines.push(`Nykyinen aktiivisuusputki: ${streak} päivää.`);
  if (bmr != null) {
    const net = Math.round(todayFoodKcal - bmr - todayExerciseKcal);
    lines.push(
      `Tänään: BMR ${bmr} kcal, liikunta +${Math.round(todayExerciseKcal)} kcal, ruoka −${Math.round(todayFoodKcal)} kcal ` +
      `→ netto ${net >= 0 ? '+' : ''}${net} kcal (negatiivinen = vaje, positiivinen = ylijäämä).`,
    );
  } else {
    lines.push('Päivän kalorien nettolaskentaan tarvittavaa profiilia/painoa ei ole vielä asetettu.');
  }

  return lines.join('\n');
}
```

**Huomio "pisin putki"/plateau-signaalista:** design-spec mainitsi valmistautumaan olemassa olevien plateau-/paras-viikko-tunnistimien uudelleenkäyttöön. Näiden asiakaspuolen (`index.html`) funktioiden suora uudelleenkäyttö ei ole mahdollista Deno-ympäristössä. Sen sijaan `recentSets`-ikkuna on asetettu 3 viikkoon (sama ikkuna kuin asiakaspuolen `checkPlateau()`:ssa) ja annetaan mallille RAAKADATANA — malli pystyy itse päättelemään onko liike jämähtänyt paikoilleen katsomalla painoja/toistoja kolmen viikon ajalta, ilman että meidän tarvitsee esilaskea plateau-lippua erikseen. Tämä on tietoinen valinta: tekoäly on nimenomaan hyvä tämän kaltaisessa päättelyssä, joten raakadatan antaminen on yksinkertaisempaa kuin logiikan siirtäminen kahteen ympäristöön.

- [ ] **Step 2: Kytke konteksti `index.ts`:ään**

Lisää tiedoston alkuun import:

```typescript
import { buildDataContext } from './context.ts';
```

Etsi `index.ts`:stä kohta jossa `messages`-taulukko rakennetaan (`const messages = history.map(...)`) ja korvaa `callClaude`-kutsua edeltävä osa:

Konteksti ennen muutosta:

```typescript
  const messages = history.map((m) => ({ role: m.role, content: m.content }));

  let reply: string;
  try {
    reply = await callClaude(COACH_SYSTEM_PROMPT, messages);
```

Korvaa:

```typescript
  const messages = history.map((m) => ({ role: m.role, content: m.content }));

  let dataContext: string;
  try {
    dataContext = await buildDataContext(sb);
  } catch (err) {
    console.error('buildDataContext failed:', err instanceof Error ? err.message : String(err));
    dataContext = '(datan haku epäonnistui, vastaa ilman sitä ja mainitse tämä käyttäjälle)';
  }
  const fullSystemPrompt = `${COACH_SYSTEM_PROMPT}\n\n---\n\nKäyttäjän data:\n${dataContext}`;

  let reply: string;
  try {
    reply = await callClaude(fullSystemPrompt, messages);
```

- [ ] **Step 3: Deployaa ja testaa datalla**

```bash
supabase functions deploy coach-chat --project-ref dodrzzgbdlucjbkmxbjn
```

Lisää uusi testiviesti (uusi `conversation_id`) joka vaatii oikeaa dataa vastatakseen oikein, esim. "Montako kertaa treenasin salilla viime viikolla?" — samalla curl-kuviolla kuin Task 2:n Step 4. Tarkista vastaus viittaa oikeaan, todelliseen lukuun (vertaa Koonti-sivun "viikon sali" -korttiin tai suoraan `workout_sets`-tauluun). Testaa myös "tämän hetken tilannekuva" -osuus erillisellä kysymyksellä, esim. "mikä on streakini ja päivän kalorit juuri nyt?" — vertaa Koonti-sivun streak- ja "päivän kalorit" -korttien lukuihin (samat luvut, sama etumerkkikonventio: negatiivinen=vaje, positiivinen=ylijäämä). Siivoa testidata samalla tavalla kuin Task 2:ssa.

- [ ] **Step 4: Committaa**

```bash
git add supabase/functions/coach-chat/context.ts supabase/functions/coach-chat/index.ts
git commit -m "feat: lisää käyttäjän datakonteksti valmentajan vastauksiin"
```

---

### Task 4: Käyttöliittymän runko — ikoni, sivupalkki, sivu, CSS

**Files:**
- Modify: `index.html` (ICONS-objekti, sivupalkki, uusi sivu-div, CSS)

- [ ] **Step 1: Lisää chat-ikoni**

Konteksti ennen muutosta (`grep -n "bell:" index.html`):

```js
  bell:      '<path d="M6 8a6 6 0 0112 0c0 7 3 9 3 9H3s3-2 3-9"/><path d="M10.3 21a1.94 1.94 0 003.4 0"/>',
};
```

Korvaa:

```js
  bell:      '<path d="M6 8a6 6 0 0112 0c0 7 3 9 3 9H3s3-2 3-9"/><path d="M10.3 21a1.94 1.94 0 003.4 0"/>',
  chat:      '<path d="M21 11.5a8.38 8.38 0 01-.9 3.8 8.5 8.5 0 01-7.6 4.7 8.38 8.38 0 01-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 01-.9-3.8 8.5 8.5 0 014.7-7.6 8.38 8.38 0 013.8-.9h.5a8.48 8.48 0 018 8v.5z"/>',
};
```

- [ ] **Step 2: Lisää sivupalkkilinkki**

Konteksti ennen muutosta (`grep -n 'showPage(.ohjelma.,null)' index.html`):

```html
  <button onclick="showPage('ohjelma',null);closeSidebar()" style="display:flex;align-items:center;gap:10px;width:100%;padding:11px 0;background:none;border:none;border-bottom:1px solid var(--border);color:var(--text);font-size:14px;cursor:pointer;">
    <span data-icon="clipboard" style="display:inline-flex"></span> Ohjelma
  </button>
  <div style="font-size:11px;font-weight:600;color:var(--text3);text-transform:uppercase;letter-spacing:.08em;margin-top:20px;margin-bottom:12px;">Asetukset</div>
```

Korvaa:

```html
  <button onclick="showPage('ohjelma',null);closeSidebar()" style="display:flex;align-items:center;gap:10px;width:100%;padding:11px 0;background:none;border:none;color:var(--text);font-size:14px;cursor:pointer;">
    <span data-icon="clipboard" style="display:inline-flex"></span> Ohjelma
  </button>
  <button onclick="showPage('valmentaja',null);closeSidebar()" style="display:flex;align-items:center;gap:10px;width:100%;padding:11px 0;background:none;border:none;border-bottom:1px solid var(--border);color:var(--text);font-size:14px;cursor:pointer;">
    <span data-icon="chat" style="display:inline-flex"></span> Valmentaja
  </button>
  <div style="font-size:11px;font-weight:600;color:var(--text3);text-transform:uppercase;letter-spacing:.08em;margin-top:20px;margin-bottom:12px;">Asetukset</div>
```

- [ ] **Step 3: Lisää sivu-div**

Konteksti ennen muutosta (`grep -n 'id="page-ohjelma"' index.html`):

```html
<div id="page-ohjelma" class="page">
  <div class="page-header">
    <button class="back-btn" onclick="showPage('koonti', document.getElementById('nav-koonti'))">‹</button>
    <span class="page-title">Ohjelma</span>
  </div>
  <div id="ohjelma-content"></div>
</div>
```

Korvaa (lisätään uusi sivu heti perään, Ohjelma-sivu itse pysyy koskemattomana):

```html
<div id="page-ohjelma" class="page">
  <div class="page-header">
    <button class="back-btn" onclick="showPage('koonti', document.getElementById('nav-koonti'))">‹</button>
    <span class="page-title">Ohjelma</span>
  </div>
  <div id="ohjelma-content"></div>
</div>

<div id="page-valmentaja" class="page">
  <div class="page-header">
    <button class="back-btn" onclick="showPage('koonti', document.getElementById('nav-koonti'))">‹</button>
    <span class="page-title">Valmentaja</span>
  </div>
  <div id="valmentaja-content"></div>
</div>
```

- [ ] **Step 4: Lisää CSS**

Konteksti ennen muutosta (`grep -n "hero-metric-label { font-size" index.html` — lisätään heti sen ryhmän jälkeen, ennen `/* ─── Koonti` -kommenttia):

Etsi rivi `.hero-metric-label { font-size:10px; color:var(--text3); margin-top:2px; }` ja tämän jälkeiset UX-korjausten yhteydessä lisätyt `.metric-modal-*`-säännöt (viimeisin niistä on `.metric-modal-bar-label`). Lisää heti niiden jälkeen, ennen `/* ─── Koonti (dashboard) ─────` -kommenttia:

```css
.coach-list-item { cursor:pointer; }
.coach-thread { display:flex; flex-direction:column; gap:10px; padding-bottom:80px; }
.coach-bubble { max-width:85%; padding:10px 14px; border-radius:14px; font-size:14px; line-height:1.4; white-space:pre-wrap; }
.coach-bubble.user { align-self:flex-end; background:var(--accent); color:#fff; border-bottom-right-radius:4px; }
.coach-bubble.assistant { align-self:flex-start; background:var(--surface2); color:var(--text); border-bottom-left-radius:4px; }
.coach-input-bar { position:fixed; bottom:0; left:0; right:0; display:flex; gap:8px; padding:10px 14px; background:var(--bg); border-top:1px solid var(--border); max-width:600px; margin:0 auto; box-sizing:border-box; }
.coach-input-bar input { flex:1; background:var(--surface2); border:1px solid var(--border); border-radius:20px; padding:10px 16px; color:var(--text); font-size:14px; min-width:0; }
.coach-input-bar button { background:var(--accent); color:#fff; border:none; border-radius:20px; padding:10px 18px; font-size:14px; cursor:pointer; flex:none; }
```

- [ ] **Step 5: Kytke `showPage()`-reititykseen**

Konteksti ennen muutosta (`grep -n "if (name === 'ohjelma')" index.html`):

```js
  if (name === 'ohjelma')   renderOhjelma();
  if (name === 'ruoka')     renderRuoka();
```

Korvaa:

```js
  if (name === 'ohjelma')   renderOhjelma();
  if (name === 'ruoka')     renderRuoka();
  if (name === 'valmentaja') renderCoachPage();
```

- [ ] **Step 6: Testaa manuaalisesti**

Lataa sovellus, avaa sivupalkki, tarkista "Valmentaja"-linkki näkyy Ohjelman alla omalla erotinviivallaan. Klikkaa sitä — sivu vaihtuu "‹ Valmentaja" -otsikkoon (konsoliin tulee `ReferenceError: renderCoachPage is not defined`, se on odotettua tässä vaiheessa, korjaantuu Task 5:ssä).

- [ ] **Step 7: Committaa**

```bash
git add index.html
git commit -m "feat: lisää Valmentaja-sivun runko, ikoni ja CSS"
```

---

### Task 5: Keskustelulista ja viestiketju

**Files:**
- Modify: `index.html` (uudet funktiot)

- [ ] **Step 1: Lisää `renderCoachPage()`, `openCoachConversation()`, `renderCoachThread()`, `startNewCoachConversation()`**

Lisää nämä neljä funktiota heti `showPage()`-funktion jälkeen (`grep -n "^function showPage" index.html` löytää sen — etsi funktion päättävä `}`-rivi ja lisää heti sen jälkeen):

```js
let coachActiveConversationId = null;

async function renderCoachPage() {
  coachActiveConversationId = null;
  const el = document.getElementById('valmentaja-content');
  el.innerHTML = '<div class="skel-sub" style="height:60px"></div>';

  const { data, error } = await sb.from('coach_messages')
    .select('conversation_id,role,content,created_at')
    .order('created_at', { ascending: true });
  if (error) {
    console.error('renderCoachPage failed:', error.message);
    el.innerHTML = '<div class="status err">Virhe ladattaessa keskusteluja</div>';
    return;
  }

  const byConv = new Map();
  (data || []).forEach(m => {
    if (!byConv.has(m.conversation_id)) byConv.set(m.conversation_id, []);
    byConv.get(m.conversation_id).push(m);
  });

  const conversations = [...byConv.entries()].map(([id, msgs]) => {
    const firstUserMsg = msgs.find(m => m.role === 'user') || msgs[0];
    return {
      id,
      title: firstUserMsg.content.slice(0, 60),
      lastAt: msgs[msgs.length - 1].created_at,
    };
  }).sort((a, b) => b.lastAt.localeCompare(a.lastAt));

  const listHtml = conversations.length
    ? conversations.map(c => `
      <div class="hist-item coach-list-item" onclick="openCoachConversation('${c.id}')">
        <div><div class="hist-label">${escapeHtml(c.title)}</div></div>
        <div style="font-size:12px;color:var(--text3)">${c.lastAt.slice(0,10)}</div>
      </div>`).join('')
    : '<div class="status">Ei vielä keskusteluja</div>';

  el.innerHTML = `
    <button class="btn btn-primary" onclick="startNewCoachConversation()" style="margin-bottom:16px;">+ Uusi keskustelu</button>
    <div id="coach-conversation-list">${listHtml}</div>
  `;
}

async function openCoachConversation(id) {
  coachActiveConversationId = id;
  const el = document.getElementById('valmentaja-content');
  el.innerHTML = '<div class="skel-sub" style="height:60px"></div>';

  const { data, error } = await sb.from('coach_messages')
    .select('role,content,created_at')
    .eq('conversation_id', id)
    .order('created_at', { ascending: true });
  if (error) {
    console.error('openCoachConversation failed:', error.message);
    el.innerHTML = '<div class="status err">Virhe ladattaessa keskustelua</div>';
    return;
  }

  renderCoachThread(data || []);
}

function renderCoachThread(messages) {
  const el = document.getElementById('valmentaja-content');
  const bubbles = messages.map(m =>
    `<div class="coach-bubble ${m.role}">${escapeHtml(m.content)}</div>`
  ).join('');
  el.innerHTML = `
    <button class="btn" onclick="renderCoachPage()" style="background:none;color:var(--text2);margin-bottom:10px;">‹ Keskustelut</button>
    <div class="coach-thread" id="coach-thread">${bubbles}</div>
    <div class="coach-input-bar">
      <input type="text" id="coach-input" placeholder="Kysy jotain..." onkeydown="if(event.key==='Enter') sendCoachMessage()">
      <button onclick="sendCoachMessage()">Lähetä</button>
    </div>
  `;
  const thread = document.getElementById('coach-thread');
  thread.scrollTop = thread.scrollHeight;
}

function startNewCoachConversation() {
  coachActiveConversationId = crypto.randomUUID();
  renderCoachThread([]);
}
```

- [ ] **Step 2: Testaa manuaalisesti**

Avaa Valmentaja-sivu — tarkista "Ei vielä keskusteluja" + "+ Uusi keskustelu" -painike näkyvät. Klikkaa painiketta — tarkista tyhjä viestiketju + syöttökenttä + "Lähetä"-painike ilmestyvät alareunaan. Kirjoittaminen/lähetys ei vielä toimi (seuraava taski), mutta navigointi "‹ Keskustelut" takaisin listaan pitää toimia.

- [ ] **Step 3: Committaa**

```bash
git add index.html
git commit -m "feat: lisää valmentajan keskustelulista ja viestiketjun renderöinti"
```

---

### Task 6: Viestin lähetys + salasanakysely

**Files:**
- Modify: `index.html` (uudet funktiot)

- [ ] **Step 1: Lisää `getCoachSecret()`, `promptCoachSecret()`, `sendCoachMessage()`**

Lisää heti Task 5:n neljän funktion jälkeen:

```js
function getCoachSecret() {
  return localStorage.getItem('coachSecret');
}

function promptCoachSecret(onSet) {
  const existing = document.getElementById('coach-secret-overlay');
  if (existing) existing.remove();

  const overlay = document.createElement('div');
  overlay.id = 'coach-secret-overlay';
  overlay.style.cssText = 'position:fixed;inset:0;z-index:200;background:rgba(0,0,0,0.7);display:flex;align-items:center;justify-content:center;padding:20px;';

  const modal = document.createElement('div');
  modal.style.cssText = 'background:var(--surface);border:1px solid var(--border2);border-radius:16px;padding:24px;max-width:360px;width:100%;';
  modal.innerHTML = `
    <div style="font-size:16px;font-weight:600;margin-bottom:16px;color:var(--text)">Valmentajan salasana</div>
    <div class="form-row"><label>Salasana</label><input type="password" id="coach-secret-input"></div>
    <button class="btn btn-primary" id="coach-secret-save-btn">Tallenna</button>
  `;
  overlay.appendChild(modal);
  document.body.appendChild(overlay);

  document.getElementById('coach-secret-save-btn').onclick = () => {
    const val = document.getElementById('coach-secret-input').value;
    if (!val) return;
    localStorage.setItem('coachSecret', val);
    overlay.remove();
    onSet();
  };
}

async function sendCoachMessage() {
  const input = document.getElementById('coach-input');
  const text = input.value.trim();
  if (!text || !coachActiveConversationId) return;

  const secret = getCoachSecret();
  if (!secret) {
    promptCoachSecret(() => sendCoachMessage());
    return;
  }

  input.value = '';
  input.disabled = true;

  const { error: insertErr } = await sb.from('coach_messages').insert({
    conversation_id: coachActiveConversationId,
    role: 'user',
    content: text,
  });
  if (insertErr) {
    console.error('sendCoachMessage insert failed:', insertErr.message);
    input.disabled = false;
    input.value = text;
    return;
  }

  const thread = document.getElementById('coach-thread');
  thread.insertAdjacentHTML('beforeend', `<div class="coach-bubble user">${escapeHtml(text)}</div>`);
  thread.insertAdjacentHTML('beforeend', `<div class="coach-bubble assistant" id="coach-pending">…</div>`);
  thread.scrollTop = thread.scrollHeight;

  let res;
  try {
    res = await fetch(`${SB_URL}/functions/v1/coach-chat`, {
      method: 'POST',
      headers: {
        'apikey': SB_KEY,
        'Authorization': `Bearer ${SB_KEY}`,
        'x-coach-secret': secret,
        'content-type': 'application/json',
      },
      body: JSON.stringify({ conversation_id: coachActiveConversationId }),
    });
  } catch (err) {
    console.error('coach-chat fetch failed:', err.message);
    res = null;
  }

  const pendingEl = document.getElementById('coach-pending');
  input.disabled = false;

  if (!res || !res.ok) {
    if (pendingEl) pendingEl.remove();
    if (res && res.status === 401) {
      localStorage.removeItem('coachSecret');
      promptCoachSecret(() => {});
    } else if (res && res.status === 429) {
      thread.insertAdjacentHTML('beforeend', `<div class="status err">Päivän viestiraja täynnä, yritä huomenna.</div>`);
    } else {
      thread.insertAdjacentHTML('beforeend', `<div class="status err">Virhe, yritä uudelleen.</div>`);
    }
    thread.scrollTop = thread.scrollHeight;
    return;
  }

  const data = await res.json();

  await sb.from('coach_messages').insert({
    conversation_id: coachActiveConversationId,
    role: 'assistant',
    content: data.reply,
  });

  if (pendingEl) {
    pendingEl.textContent = data.reply;
    pendingEl.removeAttribute('id');
  }
  thread.scrollTop = thread.scrollHeight;
}
```

- [ ] **Step 2: Testaa manuaalisesti**

Aloita uusi keskustelu Valmentaja-sivulla, kirjoita kysymys (esim. "montako kertaa treenasin salilla viime viikolla?"), lähetä. Ensimmäisellä kerralla salasanakysely avautuu — syötä Task 2:n Step 2:ssa generoitu `COACH_SECRET`-arvo. Tarkista viesti tallentuu, "…"-väliaikaisviesti ilmestyy, ja korvautuu oikealla vastauksella kun se saapuu. Lataa sivu uudelleen, avaa sama keskustelu listasta — tarkista molemmat viestit näkyvät edelleen. Testaa myös väärä salasana (tyhjennä `localStorage.removeItem('coachSecret')` konsolissa, syötä väärä arvo) — tarkista virhe näkyy eikä sovellus kaadu.

- [ ] **Step 3: Committaa**

```bash
git add index.html
git commit -m "feat: lisää valmentajan viestin lähetys ja salasanakysely"
```

---

### Task 7: Manuaalinen QA ja versionumeron päivitys

**Files:**
- Modify: `index.html` (version-chip)

- [ ] **Step 1: Koko ominaisuuden läpikäynti**

Käy läpi oikeassa selaimessa:

1. Sivupalkin "Valmentaja"-linkki toimii, sivu avautuu oikealla otsikolla.
2. Uusi keskustelu → kysymys → vastaus toimii päästä päähän, sisältäen oikean datan (kysy jotain jonka vastauksen tiedät etukäteen, vertaa oikeaan lukuun).
3. Keskustelu säilyy sivun uudelleenlatauksen yli, näkyy keskustelulistassa oikealla otsikolla ja päivämäärällä.
4. Useampi peräkkäinen viesti samassa keskustelussa toimii (konteksti säilyy — kysy jatkokysymys joka viittaa edelliseen vastaukseen).
5. Salasanaportti: väärä/puuttuva salasana estää vastauksen saamisen asianmukaisella virheellä.
6. Konsoli ei näytä virheitä normaalikäytössä.

- [ ] **Step 2: Päivitä versionumero**

Etsi `grep -n "version-chip" index.html`, vaihda kortin teksti nykyisestä `v1.19.0` arvoon `v1.20.0`.

- [ ] **Step 3: Committaa**

```bash
git add index.html
git commit -m "v1.20.0: Tekoälyvalmentaja"
```

---

## Self-Review Notes

- **Kattavuus:** Kaikki design-specin osat (interaktiomalli, arkkitehtuuri, turvallisuus, tietokanta, datakonteksti, käyttöliittymä) on katettu Task 1–6:ssa, Task 7 kokoaa QA:n ja versionumeron.
- **Riippuvuudet:** Tehtävät ovat pääosin peräkkäisiä (Task 3 rakentuu Task 2:n päälle, Task 5–6 vaativat Task 4:n sivurungon) — suositeltu järjestys 1→7, ei rinnakkaista toteutusta.
- **Tietoinen poikkeama specistä:** plateau-/paras-viikko-signaalien "uudelleenkäyttö" toteutettiin antamalla malli raakadataa (3 viikon liikekohtaiset sarjat) laskettujen lippujen sijaan — perusteltu Task 3:n Step 1:n huomiossa.
- **Ei placeholdereita:** kaikki koodilohkot täydellisiä, ei TBD/TODO-merkintöjä. Mallitunnisteen (`claude-sonnet-5`) osalta on selkeä ohje mitä tehdä jos se osoittautuu vanhentuneeksi API-kutsun epäonnistuessa.
- **Versionumero:** edellisen sub-projektin (Koonti-korttien lisätiedot) päätteeksi versio oli v1.19.0, joten tämä nostaa sen v1.20.0:aan.
